name: "Production v68 (Hybrid: Robust Sync + Health Checks)"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

concurrency:
  group: "production-bot"
  cancel-in-progress: true

jobs:
  run-v68-hybrid:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 6 Hours Limit
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      # ==================================================================
      # 1. SYSTEM TUNING
      # ==================================================================
      - name: ⚡ Optimize Environment
        run: |
          # 4GB Swap (Prevent OOM Kills)
          sudo fallocate -l 4G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile
          # Network Tuning for WebSocket Stability
          sudo sysctl -w net.core.default_qdisc=fq && sudo sysctl -w net.ipv4.tcp_congestion_control=bbr
          
          # Install Critical Deps
          sudo apt-get update && sudo apt-get install -y sqlite3 curl ffmpeg
          
          # Install Ngrok
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt-get update && sudo apt-get install -y ngrok
          
          npm install @supabase/supabase-js
          echo "✅ System Tuned"

      # ==================================================================
      # 2. CACHING (Speed Up Restarts)
      # ==================================================================
      - name: 📦 Cache Modules
        uses: actions/cache@v3
        with:
          path: |
            n8n_data/nodes
            whatsapp/node_modules
          key: bot-deps-v1-${{ runner.os }}

      # ==================================================================
      # 3. ROBUST DATA SYNC (Groq Suggestion: Add Retries)
      # ==================================================================
      - name: 💾 Setup Data Layer
        env:
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          mkdir -p n8n_data/nodes whatsapp
          
          cat << 'EOF' > smart_sync.js
          const { createClient } = require('@supabase/supabase-js');
          const fs = require('fs'); const { execSync } = require('child_process');
          const sb = createClient('https://ymatdzammnejrmmiyygg.supabase.co', process.env.SUPABASE_KEY);
          const live='n8n_data/database.sqlite', safe='n8n_data/db_snap.sqlite';

          // Retry Logic Wrapper
          async function retry(fn, retries = 3) {
            for (let i = 0; i < retries; i++) {
              try { await fn(); return; } catch (err) {
                console.error(`Attempt ${i+1} failed: ${err.message}`);
                if (i === retries - 1) throw err;
                await new Promise(r => setTimeout(r, 2000));
              }
            }
          }

          async function run(mode) {
            try {
              if(mode==='down') {
                console.log("📥 Downloading...");
                await retry(async () => {
                   const { data, error } = await sb.storage.from('bot-storage').download('database.sqlite');
                   if(error) throw error;
                   fs.writeFileSync(live, Buffer.from(await data.arrayBuffer()));
                   // Enable WAL Mode for SQLite Stability
                   try { execSync("sqlite3 " + live + " 'PRAGMA journal_mode=WAL;'"); } catch(e) {}
                });
                console.log("✅ Download Success");
              } else {
                if(!fs.existsSync(live)) return;
                // Atomic Backup
                if(fs.existsSync(safe)) fs.unlinkSync(safe);
                execSync("sqlite3 " + live + " \".backup '" + safe + "'\"");
                execSync("sqlite3 " + safe + " 'VACUUM;'"); // Shrink DB
                
                console.log("📤 Uploading...");
                await retry(async () => {
                   const { error } = await sb.storage.from('bot-storage').upload('database.sqlite', fs.readFileSync(safe), { upsert: true });
                   if(error) throw error;
                });
                console.log("✅ Upload Success");
              }
            } catch(e) { console.error("❌ Sync Fatal Error:", e.message); }
          }
          run(process.argv[2]);
          EOF
          
          node smart_sync.js down
          sudo chmod -R 777 n8n_data

      # ==================================================================
      # 4. INSTALL & CONFIGURE
      # ==================================================================
      - name: 📥 Install Nodes
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: |
          cd n8n_data/nodes && npm init -y && npm install n8n-nodes-datastore n8n-nodes-run-node-with-credentials-x n8n-nodes-tesseractjs && cd ../..
          sudo chmod -R 777 n8n_data

      # ==================================================================
      # 5. LAUNCH SERVICES (ISOLATED + SECURE)
      # ==================================================================
      - name: 🚀 Launch Bot Swarm
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Securely passing Secrets
          NGROK_AUTH: ${{ secrets.NGROK_TOKEN }}
        run: |
          # Idempotent Network
          docker network create bot-net || true
          
          # Ngrok
          ngrok config add-authtoken $NGROK_AUTH
          ngrok http --domain=${{ secrets.NGROK_DOMAIN }} 80 > /dev/null &
          sleep 5
          
          # --- SERVICE 1: AI (Zombie Mode) ---
          mkdir ai
          cat << 'EOF' > ai/ai_server.py
          import g4f, flask, logging
          app = flask.Flask(__name__)
          log = logging.getLogger('werkzeug'); log.setLevel(logging.ERROR)
          @app.route('/chat', methods=['POST'])
          def chat():
              try:
                  msg = flask.request.json.get('message')
                  if not msg: return flask.jsonify({'error': 'No message'}), 400
                  response = g4f.ChatCompletion.create(model=g4f.models.gpt_4, messages=[{'role': 'user', 'content': msg}])
                  return flask.jsonify({'response': response})
              except Exception as e: return flask.jsonify({'error': str(e)}), 500
          # Add simple health endpoint
          @app.route('/health', methods=['GET'])
          def health(): return "OK", 200
          if __name__ == '__main__': app.run(host='0.0.0.0', port=5000)
          EOF

          docker run -d --name ai-server --network bot-net --restart unless-stopped \
            -v $(pwd)/ai:/app -w /app \
            python:3.9-slim sh -c "apt-get update && apt-get install -y curl build-essential && pip install flask g4f curl_cffi && while true; do python ai_server.py; sleep 2; done"

          # --- SERVICE 2: n8n (Memory Optimized) ---
          docker run -d --name n8n --network bot-net --restart unless-stopped \
            -v $(pwd)/n8n_data:/home/node/.n8n \
            -e N8N_ENCRYPTION_KEY="${{ secrets.N8N_ENCRYPTION_KEY }}" \
            -e N8N_SECURE_COOKIE=false \
            -e WEBHOOK_URL="https://${{ secrets.NGROK_DOMAIN }}" \
            -e N8N_BLOCK_ENV_ACCESS_IN_NODE=false \
            -e NODE_OPTIONS="--max-old-space-size=2048" \
            -e DB_SQLITE_VACUUM_ON_STARTUP=true \
            -e EXECUTIONS_DATA_PRUNE=true \
            -e EXECUTIONS_DATA_MAX_AGE=24 \
            -e EXECUTIONS_DATA_SAVE_ON_SUCCESS=none \
            n8nio/n8n

          # --- SERVICE 3: WA-BOT (Media + Dedupe + Queue) ---
          echo '{"name":"wa-bot","dependencies":{"@whiskeysockets/baileys":"6.6.0","axios":"^1.6.0","express":"^4.18.2","mongoose":"^8.0.0","pino":"^8.16.1","qrcode":"^1.5.3","node-cache":"^5.1.2","async-mutex":"^0.5.0"}}' > whatsapp/package.json
          
          cat << 'EOF' > whatsapp/index.js
          const { default: makeWASocket, DisconnectReason, BufferJSON, useMultiFileAuthState, makeCacheableSignalKeyStore, delay } = require('@whiskeysockets/baileys');
          const express = require('express'); const axios = require('axios'); const mongoose = require('mongoose'); const QRCode = require('qrcode'); const pino = require('pino');
          const { Mutex } = require('async-mutex'); const NodeCache = require('node-cache');
          
          const app = express(); app.use(express.json());
          const PORT = 10000; 
          const N8N_URL = "http://n8n:5678/webhook/whatsapp"; 
          const MONGO_URL = process.env.MONGODB_URI;
          const sendMutex = new Mutex();
          const msgCache = new NodeCache({ stdTTL: 60 });
          
          const authSchema = new mongoose.Schema({ _id: String, data: Object }); 
          const AuthModel = mongoose.model('Auth_Final_v1', authSchema, 'auth_final_v1');
          
          async function useMongoDB() {
              const writeData = async (data, id) => { try { await AuthModel.findOneAndUpdate({ _id: id }, { data: JSON.parse(JSON.stringify(data, BufferJSON.replacer)), _id: id }, { upsert: true }); } catch (err) {} };
              const readData = async (id) => { try { const doc = await AuthModel.findById(id); return doc ? JSON.parse(JSON.stringify(doc.data), BufferJSON.reviver) : null; } catch (err) { return null; } };
              const removeData = async (id) => { try { await AuthModel.findByIdAndDelete(id); } catch (err) {} };
              const creds = (await readData('creds')) || (await (require('@whiskeysockets/baileys').initAuthCreds)());
              return { state: { creds, keys: { get: async (t, i) => { const d = {}; await Promise.all(i.map(async id => { let v = await readData(`${t}-${id}`); if (t === 'app-state-sync-key' && v) v = require('@whiskeysockets/baileys/lib/Utils/auth-utils').proto.Message.AppStateSyncKeyData.fromObject(v); if (v) d[id] = v; })); return d; }, set: async (d) => { const t = []; for (const c in d) for (const i in d[c]) { const v = d[c][i], k = `${c}-${i}`; t.push(v ? writeData(v, k) : removeData(k)); } await Promise.all(t); } } }, saveCreds: () => writeData(creds, 'creds'), clear: async () => { await AuthModel.deleteMany({}); } };
          }
          
          let sock; let qrData = null;
          async function start() {
              try {
                  if(mongoose.connection.readyState === 0) await mongoose.connect(MONGO_URL);
                  const { state, saveCreds, clear } = await useMongoDB(); global.clear = clear;
                  
                  console.log("🚀 WA-Bot Starting...");
                  sock = makeWASocket({ 
                      auth: { creds: state.creds, keys: makeCacheableSignalKeyStore(state.keys, pino({ level: "fatal" })) }, 
                      logger: pino({ level: 'silent' }), 
                      printQRInTerminal: false, 
                      browser: ["Windows", "Chrome", "10.0"], 
                      connectTimeoutMs: 60000,
                      syncFullHistory: false
                  });
                  
                  sock.ev.on('creds.update', saveCreds);
                  sock.ev.on('connection.update', async (u) => { 
                      const { connection, lastDisconnect, qr } = u; 
                      if(qr) qrData = qr; 
                      if(connection === 'open') { qrData = null; console.log("✅ Connected"); } 
                      if(connection === 'close') { 
                          const code = lastDisconnect.error?.output?.statusCode; 
                          if(code === DisconnectReason.loggedOut) { await clear(); process.exit(1); } 
                          else { setTimeout(start, 5000); } 
                      } 
                  });
                  
                  sock.ev.on('messages.upsert', async ({ messages }) => { 
                      const msg = messages[0]; 
                      if(!msg.message || msg.key.fromMe) return; 
                      // Deduplication
                      if (msgCache.has(msg.key.id)) return;
                      msgCache.set(msg.key.id, true);

                      try { 
                          const realNumber = (msg.key.participant || msg.key.remoteJid).split('@')[0]; 
                          axios.post(N8N_URL, { 
                              event: "message",
                              from: realNumber, 
                              text: msg.message.conversation || msg.message.extendedTextMessage?.text, 
                              name: msg.pushName, 
                              full_json: msg 
                          }).catch(()=>{}); 
                      } catch (e) {} 
                  });
                  
                  sock.ev.on('call', async (calls) => {
                      const call = calls[0];
                      if(call.status === 'offer') axios.post(N8N_URL, { event: "call", from: call.from.split('@')[0], id: call.id }).catch(()=>{});
                  });

              } catch (e) { setTimeout(start, 5000); }
          }
          
          app.post('/send', async (req, res) => { 
              try { 
                  if (!sock) return res.status(503).json({ error: "Init..." }); 
                  const { number, message, type='text', url, caption } = req.body; 
                  const fullId = number.includes('@') ? number : `${number}@s.whatsapp.net`; 
                  
                  await sendMutex.runExclusive(async () => {
                      await sock.sendPresenceUpdate('composing', fullId);
                      await delay(Math.floor(Math.random() * 500) + 500); 
                      await sock.sendPresenceUpdate('paused', fullId);
                      
                      if(type==='text') await sock.sendMessage(fullId, { text: message });
                      else if(type==='image') await sock.sendMessage(fullId, { image: { url }, caption });
                      else if(type==='video') await sock.sendMessage(fullId, { video: { url }, caption });
                      else if(type==='audio') await sock.sendMessage(fullId, { audio: { url }, mimetype: 'audio/mp4' });

                      await delay(500);
                  });
                  res.json({ status: true }); 
              } catch (e) { res.status(500).json({ error: e.message }); } 
          });
          
          // Health Check for Observer
          app.get('/health', (req, res) => res.send("OK"));
          app.get('/qr', async (req, res) => { if(sock?.user && !qrData) return res.send('<h1>✅ Connected</h1>'); if(!qrData) return res.send('<h1>⏳ Generating...</h1>'); const url = await QRCode.toDataURL(qrData); res.send(`<div style="text-align:center"><h1>Scan QR</h1><img src="${url}" /></div>`); });
          app.listen(PORT, () => { console.log(`Bot on ${PORT}`); start(); });
          EOF

          # --- SERVICE 4: REVERSE PROXY ---
          echo 'Status: v68 Online' > public/index.html
          printf ":80 {\n reverse_proxy /chat ai-server:5000\n reverse_proxy /qr wa-bot:10000\n handle /dash {\n  root * /srv/public\n  file_server\n }\n reverse_proxy /* n8n:5678\n}\n" > Caddyfile

          docker run -d --name caddy --network bot-net -p 80:80 -v $(pwd)/Caddyfile:/etc/caddy/Caddyfile -v $(pwd)/public:/srv/public caddy:latest
          
          docker run -d --name wa-bot --network bot-net --restart unless-stopped \
            -v $(pwd)/whatsapp:/app -w /app \
            -e MONGODB_URI="${{ secrets.MONGODB_URI }}" \
            node:20-alpine sh -c "apk add --no-cache git ffmpeg && npm install && while true; do node index.js; echo 'Restarting...'; sleep 2; done"

          curl -s -X POST https://api.telegram.org/bot$TG_TOKEN/sendMessage -d chat_id=$TG_ID -d text="🟢 <b>System v68 Online</b>" -d parse_mode="HTML"

      # ==================================================================
      # 6. OBSERVER LOOP (Groq Health Checks + Handover)
      # ==================================================================
      - name: 🔄 Observer Loop
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          trap 'echo "🚨 Emergency Save"; node smart_sync.js up; exit 0' EXIT
          START=$(date +%s)
          
          while true; do
             NOW=$(date +%s); DIFF=$((NOW - START))
             
             # --- 5h 50m HANDOVER ---
             if [ $DIFF -ge 21000 ]; then
                 echo "⏰ Handover Time."
                 node smart_sync.js up
                 gh workflow run main.yml
                 sleep 30
                 exit 0
             fi
             
             # --- DEEP HEALTH CHECKS (Groq Suggestion) ---
             # Check AI
             if ! docker exec ai-server curl -s http://localhost:5000/health >/dev/null; then
                 echo "⚠️ AI Unresponsive. Restarting..."
                 docker restart ai-server
             fi
             # Check WA-Bot
             if ! docker exec wa-bot curl -s http://localhost:10000/health >/dev/null; then
                  # Don't restart immediately, give it a chance to connect
                  if ! docker ps | grep -q wa-bot; then docker restart wa-bot; fi
             fi
             # Check n8n (Process check)
             if ! docker ps | grep -q n8n; then docker restart n8n; fi

             # --- DATA SYNC (Every 15 mins) ---
             if [ $((DIFF % 900)) -lt 10 ]; then node smart_sync.js up; fi
             
             echo "💓 Alive: ${DIFF}s / 21000s"
             sleep 60
          done

      - name: 🚨 Notify Failure
        if: failure()
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
           curl -s -X POST https://api.telegram.org/bot$TG_TOKEN/sendMessage -d chat_id=$TG_ID -d text="⚠️ <b>CRITICAL FAILURE</b>" -d parse_mode="HTML"
