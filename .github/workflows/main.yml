# 🤖 Ultimate WhatsApp AI Automation (GitHub Free Tier)

This is a **Production-Grade (v83)** WhatsApp Automation bot that runs 24/7 on **GitHub Actions** for FREE. It integrates **n8n**, **WhatsApp (Baileys)**, and **AI (g4f)** into a single self-healing workflow.

---

## ✨ Features
* **Zero Cost:** Runs entirely on GitHub Actions Free Tier.
* **Crash Proof:** Uses SQLite WAL mode & Supabase Cloud Backup.
* **Anti-Ban:** Uses Real-Device Signature & Auto-Fetch Versioning.
* **Auto-Healing:** Restarts automatically every 6 hours (GitHub Time Limit).
* **Smart Sync:** Backup data is encrypted and synced to Supabase.

---

## 🛠️ Prerequisites (Create these Accounts)

Before deploying, you need these 4 services ready:

1.  **GitHub Account:** To host the code.
2.  **Ngrok Account:** For the public URL (Tunnel). Get your Authtoken & create a Free Static Domain.
3.  **Supabase Account:** For file storage (Backups). Create a project & a bucket named `bot-storage`.
4.  **MongoDB Atlas:** For WhatsApp Session management.

---

## 🔐 Step 1: Configure GitHub Secrets (CRITICAL)

Go to your Repository -> **Settings** -> **Secrets and variables** -> **Actions** -> **New repository secret**.
Add these EXACT secrets:

| Secret Name | Example Value / Instruction |
| :--- | :--- |
| `GH_PAT` | Your Personal Access Token (Scope: Workflow). |
| `NGROK_TOKEN` | `2AmX...` (From Ngrok Dashboard). |
| `NGROK_DOMAIN` | `my-bot.ngrok-free.app` (Your Static Domain). |
| `MONGODB_URI` | **SEE BELOW 👇** (Most Important). |
| `SUPABASE_URL` | `https://xyz.supabase.co` (Project URL). |
| `SUPABASE_SERVICE_ROLE` | `eyJh...` (Secret Key from Supabase Settings > API). |
| `N8N_ENCRYPTION_KEY` | `any-random-password-123` (To protect n8n credentials). |
| `TELEGRAM_BOT_TOKEN` | (Optional) Get from @BotFather. |
| `TELEGRAM_CHAT_ID` | (Optional) Get from @userinfobot. |

### ⚠️ IMPORTANT: Correct MongoDB URI Format
Your URI **MUST** include the database name. Copy this format and replace your details:

```text
mongodb+srv://USER:PASSWORD@cluster0.xyz.mongodb.net/wa_bot_db?retryWrites=true&w=majority
