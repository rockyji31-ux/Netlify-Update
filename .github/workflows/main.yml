name: "Production v54 (Comfort: Auto-Rename + Key Sending)"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

concurrency:
  group: "production-bot"
  cancel-in-progress: true

jobs:
  run-v54-comfort:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      # ==================================================================
      # 1. OPTIMIZATION & NGROK
      # ==================================================================
      - name: ⚡ Tune System & Install Tools
        run: |
          sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile
          sudo sysctl -w net.core.default_qdisc=fq && sudo sysctl -w net.ipv4.tcp_congestion_control=bbr
          sudo apt-get update && sudo apt-get install -y sqlite3 curl
          
          # Install Ngrok
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt-get update && sudo apt-get install -y ngrok
          
          npm install @supabase/supabase-js
          echo "✅ System Ready"

      # ==================================================================
      # 2. CACHING
      # ==================================================================
      - name: 📦 Cache Community Nodes
        id: node-cache
        uses: actions/cache@v3
        with:
          path: n8n_data/nodes
          key: n8n-nodes-v1-${{ runner.os }}

      # ==================================================================
      # 3. SMART SYNC (WITH RENAMING & KEY SENDING)
      # ==================================================================
      - name: 💾 Setup Data & Sync Logic
        env:
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # 👇 SECRET KEY ADDED HERE FOR SCRIPT ACCESS 👇
          N8N_KEY: ${{ secrets.N8N_ENCRYPTION_KEY }}
        run: |
          mkdir -p n8n_data/nodes
          
          # --- SMART SYNC SCRIPT ---
          cat << 'EOF' > smart_sync.js
          const { createClient } = require('@supabase/supabase-js');
          const fs = require('fs'); const crypto = require('crypto'); const { execSync } = require('child_process');
          
          const live='n8n_data/database.sqlite', safe='n8n_data/db_snap.sqlite';
          const sb = createClient('https://ymatdzammnejrmmiyygg.supabase.co', process.env.SUPABASE_KEY);
          
          function hash(p) { return fs.existsSync(p) ? crypto.createHash('md5').update(fs.readFileSync(p)).digest('hex') : null; }
          
          async function run(mode) {
            try {
              if(mode==='down') {
                console.log("📥 Downloading from Supabase...");
                const { data, error } = await sb.storage.from('bot-storage').download('database.sqlite');
                if(!error) { fs.writeFileSync(live, Buffer.from(await data.arrayBuffer())); console.log("✅ Restored Hash: " + hash(live)); }
                else console.log("⚠️ Fresh Start (No Backup)");
              } else {
                if(!fs.existsSync(live)) return;
                
                try { if(fs.existsSync(safe)) fs.unlinkSync(safe); execSync("sqlite3 " + live + " \".backup '" + safe + "'\""); } 
                catch(e) { fs.copyFileSync(live, safe); }
                
                const h = hash(safe);
                if(fs.existsSync('last_hash') && fs.readFileSync('last_hash','utf8') === h) { console.log("💤 No Changes"); return; }
                
                console.log("📤 Changes Detected! Uploading...");
                const { error } = await sb.storage.from('bot-storage').upload('database.sqlite', fs.readFileSync(safe), { upsert: true });
                
                if(!error) { 
                   fs.writeFileSync('last_hash', h); 
                   console.log("✅ Supabase Sync Complete");
                   
                   // TELEGRAM BACKUP (Renamed + Key)
                   try {
                     // 1. Prepare Caption with Key
                     const caption = "💾 Backup: " + new Date().toISOString() + "\n🔑 Key: <code>" + process.env.N8N_KEY + "</code>";
                     
                     // 2. Curl Magic: ;filename=database.sqlite forces Telegram to see the new name
                     const cmd = "curl -s -F chat_id=" + process.env.TG_ID + " -F document=@" + safe + ";filename=database.sqlite -F caption=\"" + caption + "\" -F parse_mode=HTML https://api.telegram.org/bot" + process.env.TG_TOKEN + "/sendDocument";
                     
                     execSync(cmd);
                     console.log("✈️ Sent to Telegram with Key");
                   } catch(err) { console.error("Telegram Error:", err.message); }
                }
              }
            } catch(e) { console.error("Sync Error:", e.message); }
          }
          run(process.argv[2]);
          EOF
          
          node smart_sync.js down
          sudo chmod -R 777 n8n_data

      # ==================================================================
      # 4. INSTALL NODES
      # ==================================================================
      - name: 📥 Install Nodes (If not cached)
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: |
          echo "⚠️ Cache Miss! Installing nodes..."
          cd n8n_data/nodes && npm init -y && npm install n8n-nodes-datastore n8n-nodes-run-node-with-credentials-x n8n-nodes-tesseractjs && cd ../..
          sudo chmod -R 777 n8n_data

      # ==================================================================
      # 5. START SERVICES
      # ==================================================================
      - name: 🚀 Launch Services
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          URL: ${{ secrets.NGROK_DOMAIN }}
        run: |
          # Start Tunnel
          ngrok config add-authtoken ${{ secrets.NGROK_TOKEN }}
          ngrok http --domain=${{ secrets.NGROK_DOMAIN }} 80 > /dev/null &
          sleep 5
          
          # SEND START ALERT
          curl -s -X POST https://api.telegram.org/bot$TG_TOKEN/sendMessage -d chat_id=$TG_ID -d text="🟢 <b>n8n is Online!</b>%0A🔗 URL: https://$URL" -d parse_mode="HTML"
          
          docker network create bot-net

          # AI Server
          mkdir ai && echo "import g4f,flask;app=flask.Flask(__name__);@app.route('/chat',methods=['POST'])" > ai/ai_server.py
          echo "def c():try:return flask.jsonify({'response':g4f.ChatCompletion.create(model=g4f.models.gpt_4,messages=[{'role':'user','content':flask.request.json.get('message')}])}) " >> ai/ai_server.py
          echo "except Exception as e:return flask.jsonify({'error':str(e)}),500" >> ai/ai_server.py
          echo "if __name__=='__main__':app.run(host='0.0.0.0',port=5000)" >> ai/ai_server.py
          docker run -d --name ai-server --network bot-net -v $(pwd)/ai:/app -w /app python:3.9-slim sh -c "pip install flask g4f curl_cffi && python ai_server.py"

          # n8n
          docker run -d --name n8n --network bot-net \
            -v $(pwd)/n8n_data:/home/node/.n8n \
            -e N8N_ENCRYPTION_KEY="${{ secrets.N8N_ENCRYPTION_KEY }}" \
            -e N8N_SECURE_COOKIE=false \
            -e WEBHOOK_URL="https://${{ secrets.NGROK_DOMAIN }}" \
            n8nio/n8n

          # WhatsApp & Caddy
          mkdir whatsapp public
          echo '{"name":"wa-bot","dependencies":{"@whiskeysockets/baileys":"6.6.0","axios":"^1.6.0","express":"^4.18.2","mongoose":"^8.0.0","pino":"^8.16.1","qrcode":"^1.5.3","node-cache":"^5.1.2"}}' > whatsapp/package.json
          
          cat << 'EOF' > whatsapp/index.js
          const { default: makeWASocket, DisconnectReason, BufferJSON, useMultiFileAuthState, makeCacheableSignalKeyStore } = require('@whiskeysockets/baileys');
          const express = require('express'); const axios = require('axios'); const mongoose = require('mongoose'); const QRCode = require('qrcode'); const pino = require('pino');
          const app = express(); app.use(express.json());
          const PORT = 10000; const N8N_URL = "http://n8n:5678/webhook/whatsapp"; const MONGO_URL = process.env.MONGODB_URI;
          const COLLECTION_NAME = 'Auth_Final_v1'; 
          const authSchema = new mongoose.Schema({ _id: String, data: Object }); const AuthModel = mongoose.model(COLLECTION_NAME, authSchema);
          async function useMongoDB() {
              const writeData = async (data, id) => { try { await AuthModel.findOneAndUpdate({ _id: id }, { data: JSON.parse(JSON.stringify(data, BufferJSON.replacer)), _id: id }, { upsert: true }); } catch (err) {} };
              const readData = async (id) => { try { const doc = await AuthModel.findById(id); return doc ? JSON.parse(JSON.stringify(doc.data), BufferJSON.reviver) : null; } catch (err) { return null; } };
              const removeData = async (id) => { try { await AuthModel.findByIdAndDelete(id); } catch (err) {} };
              const creds = (await readData('creds')) || (await (require('@whiskeysockets/baileys').initAuthCreds)());
              return { state: { creds, keys: { get: async (t, i) => { const d = {}; await Promise.all(i.map(async id => { let v = await readData(`${t}-${id}`); if (t === 'app-state-sync-key' && v) v = require('@whiskeysockets/baileys/lib/Utils/auth-utils').proto.Message.AppStateSyncKeyData.fromObject(v); if (v) d[id] = v; })); return d; }, set: async (d) => { const t = []; for (const c in d) for (const i in d[c]) { const v = d[c][i], k = `${c}-${i}`; t.push(v ? writeData(v, k) : removeData(k)); } await Promise.all(t); } } }, saveCreds: () => writeData(creds, 'creds'), clear: async () => { await AuthModel.deleteMany({}); } };
          }
          let sock; let qrData = null;
          async function start() {
              try {
                  if(mongoose.connection.readyState === 0) await mongoose.connect(MONGO_URL);
                  const { state, saveCreds, clear } = await useMongoDB(); global.clear = clear;
                  sock = makeWASocket({ auth: { creds: state.creds, keys: makeCacheableSignalKeyStore(state.keys, pino({ level: "fatal" })) }, logger: pino({ level: 'silent' }), printQRInTerminal: false, browser: ["Shiksha AI", "Chrome", "1.0.0"], connectTimeoutMs: 60000 });
                  sock.ev.on('creds.update', saveCreds);
                  sock.ev.on('connection.update', async (u) => { const { connection, lastDisconnect, qr } = u; if(qr) qrData = qr; if(connection === 'open') { qrData = null; console.log("✅ Connected"); } if(connection === 'close') { const code = lastDisconnect.error?.output?.statusCode; if(code === DisconnectReason.loggedOut) { await clear(); process.exit(1); } else { setTimeout(start, 3000); } } });
                  sock.ev.on('messages.upsert', async ({ messages }) => { const msg = messages[0]; if(!msg.message || msg.key.fromMe) return; try { const realNumber = (msg.key.participant || msg.key.remoteJid).split('@')[0]; await axios.post(N8N_URL, { from: realNumber, text: msg.message.conversation || msg.message.extendedTextMessage?.text, name: msg.pushName, full_json: msg }); } catch (e) {} });
              } catch (e) { setTimeout(start, 5000); }
          }
          app.post('/send', async (req, res) => { try { if (!sock) return res.status(503).json({ error: "Start" }); const { number, message } = req.body; const fullId = number.includes('@') ? number : `${number}@s.whatsapp.net`; await sock.sendMessage(fullId, { text: message }); res.json({ status: true }); } catch (e) { res.status(500).json({ error: e.message }); } });
          app.get('/qr', async (req, res) => { if(sock?.user && !qrData) return res.send('<h1>✅ Connected</h1><a href="/reset">Reset</a>'); if(!qrData) return res.send('<h1>⏳ Generating...</h1>'); const url = await QRCode.toDataURL(qrData); res.send(`<div style="text-align:center"><h1>Scan QR</h1><img src="${url}" /><br/><a href="/reset">Reset DB</a></div>`); });
          app.get('/reset', async (req, res) => { if(global.clear) await global.clear(); res.send("<h1>Wiped. Restarting...</h1>"); process.exit(1); });
          app.listen(PORT, () => { console.log(`Bot on ${PORT}`); start(); });
          EOF

          # Dashboard & Caddy
          echo '<!DOCTYPE html><html><head><meta http-equiv="refresh" content="30"><title>Status</title></head><body style="background:#000;color:#0f0;text-align:center;padding:50px;"><h1>🚀 v54 Active</h1><a href="/workflow" style="color:#fff">Open n8n</a></body></html>' > public/index.html
          printf ":80 {\n reverse_proxy /chat ai-server:5000\n reverse_proxy /qr wa-bot:10000\n reverse_proxy /send wa-bot:10000\n handle /dash {\n  root * /srv/public\n  file_server\n  try_files {path} /index.html\n }\n reverse_proxy /* n8n:5678\n}\n" > Caddyfile

          docker run -d --name caddy --network bot-net -p 80:80 -v $(pwd)/Caddyfile:/etc/caddy/Caddyfile -v $(pwd)/public:/srv/public caddy:latest
          docker run -d --name wa-bot --network bot-net -v $(pwd)/whatsapp:/app -w /app -e MONGODB_URI="${{ secrets.MONGODB_URI }}" node:20-alpine sh -c "apk add --no-cache git && npm install && while true; do node index.js; echo 'Restarting...'; sleep 2; done"

      # ==================================================================
      # 6. FAST HANDOVER LOOP (5h 55m)
      # ==================================================================
      - name: 🔄 Maintenance Loop
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Also need Key here for final backup trigger
          N8N_KEY: ${{ secrets.N8N_ENCRYPTION_KEY }}
        run: |
          START=$(date +%s); TRIGGERED=false
          while true; do
             NOW=$(date +%s); DIFF=$((NOW - START))
             
             if [ $DIFF -ge 21300 ] && [ "$TRIGGERED" = false ]; then
                 echo "💾 Final Backup & Handover..."
                 node smart_sync.js up
                 echo "🔫 TRIGGERING NEXT RUNNER..."
                 gh workflow run main.yml
                 TRIGGERED=true
                 sleep 300
             fi
             
             if ! docker ps | grep -q n8n; then docker restart n8n; fi
             node smart_sync.js up
             sleep 900
          done

      # ==================================================================
      # 7. FAILURE ALERT
      # ==================================================================
      - name: 🚨 Notify Failure
        if: failure()
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
           curl -s -X POST https://api.telegram.org/bot$TG_TOKEN/sendMessage -d chat_id=$TG_ID -d text="⚠️ <b>CRITICAL: Workflow Failed!</b>%0APlease check GitHub Actions immediately." -d parse_mode="HTML"
